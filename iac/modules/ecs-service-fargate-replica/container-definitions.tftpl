[
  {
    "name": "${container_name}",
    "image": "${container_image}",
    "memory": 2048,
    "cpu": 1024,
    "essential": true,
    "portMappings": [
      {
        "containerPort": ${container_port},
        "hostPort": ${container_port}
      }
    ]
  }
]